# ──────────────────────────────────────────────
# SENTINEL Configuration
# ──────────────────────────────────────────────

# Server port (default 3000)
PORT=3000

# OPTIONAL — Get a free key at https://newsapi.org/register
# 100 requests/day free tier. App works fine without it.
NEWSAPI_KEY=

# OPTIONAL — Get an API token at https://www.thenewsapi.com
# Better international coverage (especially PH, etc). Headlines endpoint.
THENEWSAPI_KEY=

# OPTIONAL — Supabase (for user accounts & cloud sync)
# Get these from supabase.com → project → Settings → API
# App works without these in guest mode (localStorage only)
SUPABASE_URL=
SUPABASE_ANON_KEY=
